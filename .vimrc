" General

" Turn on syntax highlighting
syntax on

" Show line numbers
set number

" Show file stats
set ruler

" Blink cursoer instead of beeping
set visualbell

" Encoding
set encoding=utf-8


"" Search
" Highlight all search results
set hlsearch
" Always case-insensitive
set ignorecase
" Search for strings incrementally
set incsearch

" Auto-indent new lines
set autoindent
" Number of spaces per tab
set tabstop=4 softtabstop=4
